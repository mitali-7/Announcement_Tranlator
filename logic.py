#from moviepy.editor import TextClip
#from moviepy.config import change_settings

#UNDO THIS - LAST CHANGE
#change_settings({"IMAGEMAGICK_BINARY": "/opt/homebrew/Cellar/imagemagick/7.1.1-20/bin/convert"}) 

# Define the text and properties for the video
#text = "Hello, this is a sample text-to-video conversion."
# duration = 5  # Duration of the video in seconds
# fps = 30  # Frames per second

# Specify a valid color for the TextClip, e.g., "white"
#text_color = "white"

# Create a TextClip with the specified text and color
#clip = TextClip(text, fontsize=40, color=text_color)

# Set the duration and frame rate of the video
#clip = clip.set_duration(duration)
#clip = clip.set_fps(fps)

# Export the video as an MP4 file
#clip.write_videofile("output.mp4", codec='libx264') 